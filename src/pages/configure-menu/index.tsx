import Container from "@/layout/container";
import { default as ConfigureMenuChild } from "@/components/configure-menu";

const ConfigureMenu = () => {
  return (
    <Container>
      <ConfigureMenuChild />
    </Container>
  );
};

export default ConfigureMenu;
