import Container from "@/layout/container";
import ConfigureEmployeeContainer from "@/components/configure-employee";

const ConfigureEmployee = () => {
  return (
    <Container>
      <ConfigureEmployeeContainer />
    </Container>
  );
};

export default ConfigureEmployee;
